#include <stdio.h>
int main() {
    char name[50]={0};
    int i=0,count=1;
    printf("enter a word: ");
    scanf("%s",&name);

    while(name[i]!='\0')
    {
       printf("%c \n" ,name[i]);
       i++;
    }
}
